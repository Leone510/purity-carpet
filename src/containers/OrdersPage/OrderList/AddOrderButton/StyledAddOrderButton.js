import styled from 'styled-components';
import { StyledButon } from '../../../../components/Button/StyledButton';

export const StyledAddOrderButton = styled(StyledButon)`
   width: 100%;
   color: var(--primary-white);
   cursor: pointer;
`