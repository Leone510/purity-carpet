import styled from 'styled-components';
import { StyledButon } from '../../../../../components/Button/StyledButton';

export const StyledRemoveOrderButton = styled(StyledButon)`
   width: 30px;
   height: 100%;
   border: none;
   border-radius: 0;
   color: var(--primary-white);
   font-size: 20px;
`