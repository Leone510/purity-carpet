import styled from 'styled-components';

export const StyledButon = styled.button`
   height: 30px;
   width: 100px;
   background: var(--primary-hight-light);
   border: 1px solid var(--primary-dark);
   color: var(--primary-dark);
   border-radius: 5px;
   cursor: pointer;
`